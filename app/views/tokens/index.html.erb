<h1>トークン一覧</h1>
<div class="container-user-info">
  <%= @account_student_id %>
  <%= link_to  "ログアウト", session_path(@account), method: :delete %>
</div>
<% if @tokens && @tokens.size > 0 %>
  <table>
    <tbody>
      <tr>
        <th>トークン</th>
        <th>有効状態</th>
      </tr>

        <% @tokens.each do |t| %>
          <tr>
            <td>
              <%= t.token %>
            </td>
            <td>
              <%= t.available ? "有効" : "無効" %>
            </td>
            <% if t.available %>
            <td>
              <%= link_to "無効にする", token_path(t), method: :delete %>
            </td>
            <% end %>
          </tr>
        <% end %>
    </tbody>
  </table>
<% else %>
  関連付けられたトークンはありません。
<% end %>
<%= link_to "新規作成", :tokens, method: :post %>